// Vulkan 1.2 core device functions used by native title renderers.
XE_UI_VULKAN_FUNCTION(vkGetBufferDeviceAddress)
// Promoted to core in Vulkan 1.1. Native title renderers require Vulkan 1.2,
// so it is loaded with the rest of their core device entry points.
XE_UI_VULKAN_FUNCTION(vkGetDescriptorSetLayoutSupport)
